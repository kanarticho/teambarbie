{% extends 'base.html.twig' %}

{% block title %}Moodday{% endblock %}

{% block body %}
    <h1>Moodday</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moodday.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ moodday.date ? moodday.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moodday_index') }}">back to list</a>

    <a href="{{ path('moodday_edit', {'id': moodday.id}) }}">edit</a>

    {{ include('moodday/_delete_form.html.twig') }}
{% endblock %}
