{% extends 'base.html.twig' %}

{% block title %}Patient index{% endblock %}

{% block body %}
    <h1>Patient index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Medic'Alert</th>
                <th>Mood'Alert</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for patient in patients %}
            <tr>
                <td>{{ patient.firstname }}</td>
                <td>{{ patient.lastname }}</td>
                <td>
                    MedicAlert
                </td>
                <td>
                    MoodAlert
                </td>
                <td>
                    <a href="{{ path('patient_show', {'id': patient.id}) }}">Medication</a>
                    <a href="{{ path('patient_show', {'id': patient.id}) }}">Show</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
